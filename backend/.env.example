# -------------------------------------------------
# Backend environment variables – example file
# -------------------------------------------------
# PostgreSQL connection string
# Replace the placeholders with your actual database credentials.
#   POSTGRES_USER     – database username (e.g., postgres)
#   POSTGRES_PASSWORD – database password (e.g., your_password)
#   POSTGRES_HOST     – host address (e.g., 192.168.86.243)
#   POSTGRES_PORT     – port number (default 5432)
#   POSTGRES_DB       – database name (e.g., realestate)

DATABASE_URL="postgresql://POSTGRES_USER:POSTGRES_PASSWORD@POSTGRES_HOST:POSTGRES_PORT/POSTGRES_DB"
